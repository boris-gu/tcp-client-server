# Клиент/Сервер (протокол TCP)
Вызов сервера (сервер выводит порт):  
java Main -s

Вызов клиента:  
java Main -c localhost [порт]

Сервер может работать с несколькими клиентами.  Сервер принимает от клиента сообщение и отправляет обратно его же, но в верхнем регистре.
